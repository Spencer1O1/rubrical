# Copy to .env.local and edit. Loaded automatically (.env.local overrides .env).
# See docs/development.md for details.

# Server
# RUBRICAL_ADDR=:8787
DATABASE_URL=postgres://rubrical:rubrical@localhost:5432/rubrical?sslmode=disable

# Draft file bytes on disk
RUBRICAL_DATA_DIR=./data

# Extension extraction: set 1 to disable Canvas fallbacks (dev/testing)
RUBRICAL_STRICT_EXTRACTION=0

# Purge uploaded draft files after retention (Go duration: 168h, 720h, 0 to disable a rule)
POST_DUE_DATE_RETENTION_TIME=168h
POST_UPLOAD_RETENTION_TIME=720h

# Draft files — max bytes per file (upload, import, and AI analysis share this limit)
# DRAFT_MAX_FILE_BYTES=33554432
# DRAFT_MAX_FILES_PER_DRAFT=20

# AI — beta uses per-user keys saved in Settings (dashboard or extension popup).
# AI_PROMPT_MAX_DRAFT_CHARS=120000
# Rate limits are off by default during BYOK beta; enable for hosted/subscription mode:
# AI_ENFORCE_RATE_LIMITS=1
# AI_MAX_RUNS_PER_HOUR=10
# AI_MAX_RUNS_PER_DAY=50
# AI_MIN_SECONDS_BETWEEN_RUNS=30
